/*
details = {
    text: "Active",
}
detailsF = {
    text: "fail",
};

var url = window.location.href;
var regex = new RegExp(/^https:\/\/order\.dominos\.ca.*$/);
console.log(url);

if(regex.test(url)) {
  chrome.browserAction.setBadgeText(details);
} else {
  chrome.browserAction.setBadgeText(detailsF);
} 

*/
